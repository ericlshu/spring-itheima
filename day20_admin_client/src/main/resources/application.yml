server:
  port: 80

spring:
  boot:
    admin:
      client:
        url:
          - http://localhost:8080

management:
  endpoint:
    health:
      show-details: ALWAYS
  endpoints:
    web:
      exposure:
        include:
          - "*"
